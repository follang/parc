# AST Model

The `ast` module defines the complete C abstract syntax tree.

## Top level

- `TranslationUnit` — root node containing a list of `ExternalDeclaration`
- `ExternalDeclaration` — a function definition or declaration

## Declarations

- `Declaration` — type specifiers + init declarators
- `Declarator` — names and derived types (pointers, arrays, functions)
- `TypeSpecifier` — `Void`, `Int`, `Float`, `Struct`, `Enum`, `TypedefName`, etc.
- `TypeQualifier` — `Const`, `Volatile`, `Restrict`, `Atomic`
- `StorageClassSpecifier` — `Typedef`, `Extern`, `Static`, `Auto`, `Register`

## Expressions

- `Expression` — full expression tree (binary, unary, cast, call, member access, etc.)
- `Constant` — integer, float, and character literals

## Statements

- `Statement` — compound, if/else, for, while, do-while, switch, return, goto, labeled, asm

## Extensions

- `Attribute` — GCC/Clang attributes
- `AsmStatement` — inline assembly
- `TypeOf` — `typeof` expressions (GNU)
