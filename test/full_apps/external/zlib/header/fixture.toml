name = "zlib-header"
mode = "driver"
flavor = "gnu"
entry = "main.c"
include_dirs = ["include"]
expected = "parse_ok"
source = "https://github.com/madler/zlib"
upstream_ref = "v1.3.1"
license = "Zlib"
notes = "Vendored official zlib public headers with a local wrapper translation unit."
tags = ["external", "header", "zlib"]
